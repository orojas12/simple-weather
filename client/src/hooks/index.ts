import { useLocation } from "./useLocation";
import useWeather, {
  WeatherContext,
  WeatherData,
  Weather,
  WeatherCurrent,
  WeatherDay,
  WeatherHour,
  WeatherAlert,
} from "./useWeather";

export type { WeatherData };
export {
  useLocation,
  useWeather,
  WeatherContext,
  Weather,
  WeatherCurrent,
  WeatherDay,
  WeatherHour,
  WeatherAlert,
};
