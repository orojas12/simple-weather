import useLocation, { Location, Coords, LocationContext } from "./useLocation";
import useWeather from "./useWeather";

export type { Location, Coords };
export { useLocation, LocationContext, useWeather };
